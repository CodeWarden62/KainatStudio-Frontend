import { TypeSafeMateCellDefDirective } from './type-safe-mate-cell-def.directive';

describe('TypeSafeMateCellDefDirective', () => {
  it('should create an instance', () => {
    const directive = new TypeSafeMateCellDefDirective();
    expect(directive).toBeTruthy();
  });
});
